- name: install cron job (ansible-pull)
  cron:
    user: ansible
    name: "ansible provision"
    minute: "*/5"
    job: "/usr/bin/ansible-pull -o -i ~/edgesec.ca/devops_pull/hosts.ini -U git@github.com:CEP-Comwell/devops_pull.git >> /dev/null"
